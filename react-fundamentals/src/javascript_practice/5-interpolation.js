const username = 'Kenn';
console.log(`${username} is the whatever idk`);

console.log(`Hey students,
what bugs can 
${username}
fix for you
today`); //breaks this down to multiple lines!

const person = {
    username: 'Kenn',
    profession: 'secret agent'
}
console.log(`${person.username}, ${person.profession}`, 'Extraordinaire');